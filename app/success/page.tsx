'use client';
import Header from '@/components/Header';import{useEffect,useState}from'react';
export default function Success(){const[report,setReport]=useState('Generating your Claude AI report...');useEffect(()=>{const answers=JSON.parse(localStorage.getItem('spendshift_answers')||'[]');fetch('/api/generate-report',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers})}).then(r=>r.json()).then(d=>setReport(d.report||d.error||'Report generated.')).catch(()=>setReport('Could not generate report. Check ANTHROPIC_API_KEY.'))},[]);return <div className="auditPage"><Header simple/><main className="success"><div className="greenPanel"><h1>Payment successful ✅</h1><p>Your unlocked savings report is ready below.</p></div><h2>Your Full SpendShift Report</h2><div className="reportText">{report}</div></main></div>}
